✅git remote add origin URL
新しく「origin」というリモート接続を登録する
プロジェクトにまだ一度もリモートを設定していないとき（初回）

✅git remote set-url origin URL
既にoriginと言う名前がついているリモートリポジトリ(既に一度はローカルとの接続に成功しているリポジトリ)にローカルリポジトリの接続先を変える場合

※一度はローカルとひもずいたリモートリポジトリはデフォルトでoriginと言う名前がつく